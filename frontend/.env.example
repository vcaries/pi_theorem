# Frontend configuration (copy to `.env` to override).
# Base URL of the Pi-Scope API. Leave unset in development to use the Vite
# proxy (/api -> http://127.0.0.1:8000). Set explicitly for production builds.
# VITE_API_URL=http://localhost:8000/api
